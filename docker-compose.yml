version: '3'
services:
  https-portal:
    image: steveltn/https-portal:1
    ports:
      - '80:80'
      - '443:443'
    environment:
      DOMAINS: 'everyone:engineer@team1.java-exam.com.35.213.80.42.nip.io -> http://host.docker.internal:8080'
      STAGE: 'staging'
    volumes:
      - https-portal-data:/var/lib/https-portal
    extra_hosts:
      - "host.docker.internal:host-gateway"
volumes:
  https-portal-data:
